export interface ModelUnResidenteI {
    per_id: string,
    per_nombres: string,
    per_apellidos: string,
    rol_descripcion: string,
    rol_id: number,
    res_id: number,
    res_correo: string,
    res_telefono: string,
}